@model IEnumerable<Proyecto_Effetrans.Models.Usuario>

<select class="form-control" id="IdUsuario" name="IdUsuario">
    @foreach(var item in Model)
            {
                <option value="@item.IdUsuario">@item.Nombre</option>
            }
</select>
